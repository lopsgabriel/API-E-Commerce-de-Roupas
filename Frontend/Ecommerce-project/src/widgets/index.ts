import { LayoutFooter } from "./LayoutFooter";
import { LayoutHeader, Logo, Menu } from "./LayoutHeader";

/**
 * Exporta os componentes de layout utilizados na aplicação.
 * 
 * - **LayoutHeader**: Componente que contém a estrutura do cabeçalho da aplicação, incluindo o logo e o menu.
 * - **Logo**: Componente que exibe o logo da aplicação dentro do cabeçalho.
 * - **Menu**: Componente de navegação que exibe o menu dentro do cabeçalho.
 * - **LayoutFooter**: Componente que contém o rodapé da aplicação.
 */
export { LayoutHeader, Logo, Menu, LayoutFooter };
